
export type Referrer = {
    id: string,
    stripeExpressAccountID: string,
    activeCouponID: string,
    activePayStructureID: string,
};

export type User = {
    id: string,
    firstName: string,
    lastName: string,
    email: string
};
